---
description: 团队 LLM Wiki 治理规则。涉及 wiki 查询/入库/同步时 @-mention 本规则;知识库是本地文件,直接读 + 用 wiki-cli。
alwaysApply: false
---

# 团队 LLM Wiki(Cursor 适配)

> 本规则随 flux-wiki 插件分发(路 A / 插件市场版),作 @-mention 便利。知识库是本地 markdown,**直接读 + 调 wiki-cli**。
> ⚠ 同步提示:本规则正文与 wiki-init 版 `adapters/cursor/wiki.mdc` 语义一致,改正文请同步那份;两份仅路径机制不同(此处 `${*_PLUGIN_ROOT}` vs 那份 `{{占位符}}`)。

下文 `wiki-cli` 指 `python3 "${CLAUDE_PLUGIN_ROOT:-${CURSOR_PLUGIN_ROOT:-${PLUGIN_ROOT}}}/tools/bin/wiki-cli"`(插件根变量:Cursor 为 `CURSOR_PLUGIN_ROOT`,CC/Codex 为 `CLAUDE_PLUGIN_ROOT`)。命令正文沿用 wiki-cli 简写。

知识库位置由 `WIKI_ROOT` 环境变量决定(未设时 wiki-cli 兜底约定路径 `~/AI/team-wiki`);多库用 `--root` 切换。

涉及 wiki 时:
1. 协议与分类闭集:`wiki-cli protocol`;或直接读库根的 `AGENTS.md` + `_vocabulary.md`。
2. 查知识:`wiki-cli search/route/get`,或直接 Grep/Read 库下的 `.md`,引用页面路径。
3. 入库(仅维护者):`wiki-cli suggest` → 补 frontmatter → `wiki-cli validate` 通过后落盘;低置信入 `staging/`。
4. 同步团队知识到个人库:`wiki-cli sync-team --team <团队仓clone>`。(以上为 WIKI_ROOT=个人库 的日常主线;若 WIKI_ROOT 指向团队仓 clone 才用 `wiki-cli changes` / git pull 看更新)

规则真源:库根的 `AGENTS.md` + `_vocabulary.md`。wiki-cli:`${CURSOR_PLUGIN_ROOT}/tools/bin/wiki-cli`。

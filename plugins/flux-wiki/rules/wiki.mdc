---
description: 团队 LLM Wiki 治理规则。涉及 wiki 查询/入库/同步时 @-mention 本规则;知识库是本地文件,直接读 + 用 wiki-cli。
alwaysApply: false
---

# 团队 LLM Wiki(Cursor 适配)

> 本规则随 flux-wiki 插件分发(路 A / 插件市场版),作 @-mention 便利。知识库是本地 markdown,**直接读 + 调 wiki-cli**。
> ⚠ 同步提示:本规则正文与 wiki-init 版 `adapters/cursor/wiki.mdc` 语义一致,改正文请同步那份;两份仅路径机制不同(此处 `${*_PLUGIN_ROOT}` + 运行时 `WIKI_ROOT` vs 那份安装时渲染好的 `{{占位符}}` 绝对路径)。

本机有两个本地知识库:**个人库**(日常读写主线,由 `WIKI_ROOT` 环境变量决定,未设时兜底约定 `~/AI/wiki`)与**团队仓**(只读知识源的本地 clone,约定 `~/AI/team-wiki`)。多库用 `--root` 切换。
> 插件市场路径无法在安装时把库的绝对路径烧进规则;想要交互式配好两个库,走主路径 `./install.sh`(见仓库 README)。

下文 `wiki-cli` 指 `python3 "${CLAUDE_PLUGIN_ROOT:-${CURSOR_PLUGIN_ROOT:-${PLUGIN_ROOT}}}/tools/bin/wiki-cli"`(插件根变量:Cursor 为 `CURSOR_PLUGIN_ROOT`,CC/Codex 为 `CLAUDE_PLUGIN_ROOT`)。命令正文沿用 wiki-cli 简写。

涉及 wiki 时:
1. 协议与分类闭集:`wiki-cli protocol`;或直接读个人库根的 `AGENTS.md` + `_vocabulary.md`。
2. 查知识:`wiki-cli search/route/get`,或直接 Grep/Read 个人库下的 `.md`(含团队镜像 `team/` 区),引用页面路径。也可直接查团队仓 `~/AI/team-wiki`。
3. 入库(仅维护者):`wiki-cli suggest` → 补 frontmatter → `wiki-cli validate` 通过后落盘个人库;低置信入 `staging/`。
4. 看团队仓更新 / 同步:`wiki-cli --root ~/AI/team-wiki changes`(只读)或 `git -C ~/AI/team-wiki pull`;`wiki-cli sync-team --team ~/AI/team-wiki` 镜像进个人库。

规则真源:个人库根的 `AGENTS.md` + `_vocabulary.md`。wiki-cli:`${CURSOR_PLUGIN_ROOT}/tools/bin/wiki-cli`。

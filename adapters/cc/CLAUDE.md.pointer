<!-- 由 wiki-init 注册到 ~/.claude/CLAUDE.md(用户级规则/内存)。仅指针,不复制协议正文。占位符由 wiki-init 替换。 -->

## 团队 LLM Wiki(flux-wiki)

本机有两个本地知识库,别混(都是本地 markdown,**直接读 + 用 wiki-cli**):
- **个人库**(日常读写主线,你的入库/查询都在这):`{{WIKI_ROOT}}`
- **团队仓**(只读知识源,团队知识仓的本地 clone):`{{TEAM_ROOT}}`

下文 `wiki-cli` 指 `python3 {{WIKI_MANAGE}}/plugins/flux-wiki/tools/bin/wiki-cli`。命令正文沿用 wiki-cli 简写。

涉及该 wiki 的查询/入库/同步时:
- 协议与分类闭集:读 `{{WIKI_ROOT}}/AGENTS.md` + `{{WIKI_ROOT}}/_vocabulary.md`(规则真源),或跑 `wiki-cli --root "{{WIKI_ROOT}}" protocol`。
- 查知识:直接 Grep/Read `{{WIKI_ROOT}}` 下的 `.md`(含 sync 进来的团队镜像 `team/` 区);或 `wiki-cli --root "{{WIKI_ROOT}}" search/route/get`。也可直接查团队仓 `{{TEAM_ROOT}}` 下的 `.md`。
- 入库(仅维护者):`wiki-cli suggest` 定落位 → 补 frontmatter → `wiki-cli validate` 通过后用 file tools 落盘到个人库;低置信入 `staging/`;轻量加页 `wiki-cli new`。
- 看团队仓更新:`wiki-cli --root "{{TEAM_ROOT}}" changes`(只读 git fetch+diff);拉取更新:`git -C "{{TEAM_ROOT}}" pull`。
- 同步团队知识进个人库:`wiki-cli --root "{{WIKI_ROOT}}" sync-team --team "{{TEAM_ROOT}}"`(原样镜像到个人库 `team/` 区)。

wiki-cli:`{{WIKI_MANAGE}}/plugins/flux-wiki/tools/bin/wiki-cli`(纯 Python 标准库)。

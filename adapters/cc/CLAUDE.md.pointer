<!-- 由 wiki-init 注册到 ~/.claude/CLAUDE.md(用户级规则/内存)。仅指针,不复制协议正文。占位符由 wiki-init 替换。 -->

## 团队 LLM Wiki(wiki-governance)

本机维护一个 LLM Wiki(知识库),根目录 `{{WIKI_ROOT}}`。它是本地 markdown,**直接读 + 用 wiki-cli**,不经 MCP。涉及该 wiki 的查询/入库/同步时:
- 协议与分类闭集:读 `{{WIKI_ROOT}}/AGENTS.md` + `{{WIKI_ROOT}}/_vocabulary.md`(规则真源),或跑 `wiki-cli --root "{{WIKI_ROOT}}" protocol`。
- 查知识:直接 Grep/Read `{{WIKI_ROOT}}` 下的 `.md`;或 `wiki-cli --root "{{WIKI_ROOT}}" search/route/get`。
- 入库(仅维护者):`wiki-cli suggest` 定落位 → 补 frontmatter → `wiki-cli validate` 通过后用 file tools 落盘;低置信入 `staging/`;轻量加页 `wiki-cli new`。
- 同步团队知识到个人库:`wiki-cli --root "{{WIKI_ROOT}}" sync-team --team <团队仓clone>`(原样镜像到个人库 `team/` 区)。

wiki-cli:`{{WIKI_MANAGE}}/plugins/wiki-governance/tools/bin/wiki-cli`(纯 Python 标准库)。

# LLM Wiki 协议指针(放 ~/.codex/AGENTS.md —— 用户级,跨工作区生效)

> 仅指针,不复制协议正文(防漂移)。占位符由 bin/wiki-init 替换。

本机有两个本地知识库(都是本地 markdown,**直接读 + 用 wiki-cli**):
- **个人库**(日常读写主线):`{{WIKI_ROOT}}`
- **团队仓**(只读知识源,团队知识仓本地 clone):`{{TEAM_ROOT}}`

下文 `wiki-cli` 指 `python3 {{WIKI_MANAGE}}/plugins/flux-wiki/tools/bin/wiki-cli`。命令正文沿用 wiki-cli 简写。

任务涉及该 wiki 的查询/入库/同步时:
1. 协议与分类闭集:读 `{{WIKI_ROOT}}/AGENTS.md` + `{{WIKI_ROOT}}/_vocabulary.md`;或 `wiki-cli --root "{{WIKI_ROOT}}" protocol`。
2. 查知识:直接 Grep/Read `{{WIKI_ROOT}}` 下的 `.md`(含团队镜像 `team/` 区);或 `wiki-cli --root "{{WIKI_ROOT}}" search/route/get`。也可直接查团队仓 `{{TEAM_ROOT}}`。
3. 入库(仅维护者):`wiki-cli suggest` → 补 frontmatter → `wiki-cli validate` 通过后用编辑器落盘个人库;低置信入 `staging/`。
4. 看团队仓更新:`wiki-cli --root "{{TEAM_ROOT}}" changes`(只读);拉取 `git -C "{{TEAM_ROOT}}" pull`。
5. 同步团队知识进个人库:`wiki-cli --root "{{WIKI_ROOT}}" sync-team --team "{{TEAM_ROOT}}"`。

wiki-cli:`{{WIKI_MANAGE}}/plugins/flux-wiki/tools/bin/wiki-cli`。规则真源:`{{WIKI_ROOT}}/AGENTS.md` + `{{WIKI_ROOT}}/_vocabulary.md`。

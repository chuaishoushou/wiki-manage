<!-- 放到团队仓 team-wiki/.claude/CLAUDE.md —— 只做指针,不抄协议正文(防漂移) -->

# 本仓是团队 LLM Wiki

任何 wiki 操作前,先读本仓根的:
- `AGENTS.md` —— 操作协议 SSOT
- `_vocabulary.md` —— 受控词表(合法 domain / page_type / tag 白名单 / 敏感度 / 必填集)
- `_routes.md` —— 关键词路由表
- `overview.md` —— 顶层地图

或用 `wiki-cli protocol` 拿协议摘要 + 版本 + 新鲜度(也可直接 Read 上述本地 .md 文件)。

> 规则正文只活在 `AGENTS.md` + `_vocabulary.md`。本文件、plugin、各平台适配层一律只放指针。

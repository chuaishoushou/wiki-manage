# 仓库模型:团队仓 vs 个人库(必读,防误用)

> 这页解决最常见的两个误解:① 把团队仓"并进"个人库;② `git pull`(拉取)后又"重新分类一遍"。

## 一个仓库有三种角色,别混

| 角色 | 在哪 | 谁写 | 你怎么用 |
|---|---|---|---|
| **团队仓(SSOT,唯一真源)** | git(版本控制)远端 | 1-2 名维护者 | 受保护分支,成员无 push(推送)权 |
| **团队仓的本地 clone(克隆副本)** | 你本地一个目录 | 没人(只读) | `git clone`(克隆)+ `git pull`(拉取),AI **只读**,**永不往里 ingest(入库消化)** |
| **个人库(可选)** | 你本地另一个目录 | 你自己 | 随便加,**永不 push 到团队仓** |

**团队仓和个人库是两个独立目录、两个独立 `WIKI_ROOT`(库根环境变量),不嵌套、不合并。** 尤其别用 git submodule(子模块)把团队仓塞进个人仓。

## 重活只发生一次:在团队仓的入口

"深刻理解 + 完整分类"这套重流程,**只在维护者把全新原始资料 ingest(入库消化)进团队仓时发生,且只发生一次**。

| 场景 | 要不要完整 ingest(入库消化)? |
|---|---|
| 维护者把**新原始资料**放进团队仓 | **要**(唯一需要的地方) |
| 成员 `git pull`(拉取)后消费团队仓 | **不要**,已是成品,直接读 |
| 你往**个人库**记笔记 | **看你**,轻量即可 |

### ❌ 反模式:`git pull` 后"再 ingest 一遍"
团队仓里的页**早已分类好**(每页有 frontmatter(元数据头)、落在正确 domain(领域)、进了路由)。拉下来就是拿成品,**直接读**。再跑一遍 ingest(入库消化)= 同一份知识在每人本地重复加工、还可能各自加工出不同结果,既冗余又破坏 SSOT(唯一真源)。**绝不要这么做。**

## 两种"添加知识",轻重不同

- **轻(个人库 / 已知归属)**:`wiki-cli new <type> <slug> --domain <d>` 一条命令生成合规页骨架,填正文即可。不必跑完整流程。
- **重(进团队仓)**:走 `/wiki-ingest`(skill 技能):敏感度闸 → 分类决策树 → 校验 → 落盘 + 审计。由维护者把关。

## 个人知识想进团队仓:走贡献回路,不是 pull
成员产生的新知识若要进团队仓,**经 PR(Pull Request,合并请求)提到团队仓的 `staging/`(暂存区),由该 domain 的 owner(负责人)审核晋升**——不是在自己 `git pull` 时混进来。

## 落到工具(已支持,无需额外功能)
```bash
git clone https://github.com/chuaishoushou/team-wiki.git ~/AI/team-wiki        # 团队库:只读消费
wiki-cli init ~/my-wiki --domains 笔记     # 个人库:另开一个
wiki-cli --root ~/AI/team-wiki  search "..."  # 查团队
wiki-cli --root ~/my-wiki    new concept x --domain 笔记   # 往个人库轻量加页
```
MCP(模型上下文协议)侧配两条:`wiki-team`(WIKI_ROOT 指团队 clone,只读消费)、`wiki-personal`(WIKI_ROOT 指个人库,可写)。
